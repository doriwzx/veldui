// Hypothetical function definition or usage
function scale$4(mode1, mode2, options) {
    // Implementation logic would depend on the specific context
    // Here's a simplified example to illustrate possible usage
    console.log(`Scaling with modes: ${mode1}, ${mode2}`);
    console.log(`Options: ${options}`);
}

// Calling scale$4 with specific arguments
scale$4('Sequential', 'sequential', ['Continuous', 'Interpolating']);
// Output:
// Scaling with modes: Sequential, sequential
// Options: Continuous,Interpolating
